# 2026-02-27 — Архитектурное Решение: Tools vs Resources vs Prompts

**Статус:** ✅ Утверждено  
**Участники:** User (Team Lead), Архитектор

---

## Проблема

**Мой вопрос Архитектору:**
> Есть ощущение что Tools, Resources и Prompts дублируют друг друга. 
> Не будет ли дублирование проблемой для клиента? Может сконцентрироваться только на Tools?

---

## Исследование Архитектора

### 1. Концептуальная разница (MCP спецификация)

| Компонент | Назначение | Парадигма | Инициация |
|-----------|------------|-----------|-----------|
| **Tools** | Выполнение действий | Action-oriented | LLM вызывает |
| **Resources** | Чтение данных | Data-oriented | Клиент читает |
| **Prompts** | Шаблоны workflows | Workflow-oriented | Пользователь выбирает |

### 2. Выявленные проблемы в нашей архитектуре

**Дублирование:**
```
❌ analyze_commits (Tool) ↔ git://commits (Resource)
   → Оба возвращают данные коммитов

❌ generate_changelog (Tool) ↔ git://changelog (Resource)
   → Tool генерирует, Resource читает (размытая граница)

❌ prompt://draft-release ↔ generate_release_notes (Tool)
   → Prompt дублирует логику вызова Tool
```

### 3. Оценка для AI-клиентов

| Сценарий | Tools Only | Tools + Resources | Полный контракт |
|----------|------------|-------------------|-----------------|
| Простота интеграции | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Гибкость | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Понятность для LLM | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Баллы хакатона** | ~60/100 | ~85/100 | **~95/100** |

---

## Архитектурное Решение

### ✅ Выбран подход: **Вариант A+ (Модифицированный)**

**Оставить все 3 компонента с ЧЁТКИМ разделением:**

```
┌─────────────────────────────────────────────────────────┐
│  НОВАЯ АРХИТЕКТУРА                                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  TOOLS (Действия):                                      │
│  ├── analyze_commits()     → Анализ + статистика        │
│  ├── generate_changelog()  → Генерация файла/текста     │
│  └── generate_release_notes() → Release notes           │
│                                                          │
│  RESOURCES (Контекст/Чтение):                           │
│  ├── git://tags/{repo}     → Список версий              │
│  ├── git://commits/{repo}  → Сырые коммиты              │
│  └── git://stats/{repo}    → Быстрая статистика         │
│                                                          │
│  PROMPTS (Рабочие процессы):                            │
│  ├── draft-release         → Чеклист подготовки         │
│  └── summarize-changes   → Резюме для менеджеров        │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### Критерии выбора компонента

| Вопрос | Ответ | Компонент |
|--------|-------|-----------|
| Нужно **выполнить действие** с параметрами? | Да | **Tool** |
| Нужно **прочитать данные** по идентификатору? | Да | **Resource** |
| Нужно **следовать шаблону** для задачи? | Да | **Prompt** |

---

## Обновлённая Структура Компонентов

### Tools (3) — Действия

```python
@mcp.tool()
async def analyze_commits(repo_path, from_ref, to_ref) -> dict:
    """Анализ коммитов с категоризацией и статистикой"""
    # Возвращает: commits, summary, stats, breaking_changes

@mcp.tool()
async def generate_changelog(repo_path, output_format, from_version) -> str:
    """Генерация CHANGELOG в указанном формате"""
    # Возвращает: готовый текст для файла

@mcp.tool()
async def generate_release_notes(repo_path, version, style) -> dict:
    """Release notes для публикации"""
    # Возвращает: {title, content, breaking_changes, migration_guide}
```

### Resources (3) — Контекст/Чтение

```python
@mcp.resource("git://tags/{repo_path}")
async def list_tags(repo_path) -> list[dict]:
    """Список всех тегов — только мета-данные"""
    # Возвращает: [{name, commit, date}, ...]

@mcp.resource("git://commits/{repo_path}/{from_ref}/{to_ref}")
async def get_raw_commits(repo_path, from_ref, to_ref) -> list[dict]:
    """Сырые коммиты для прямого доступа LLM"""
    # Возвращает: [{hash, message, author, date}, ...]

@mcp.resource("git://stats/{repo_path}/{from_ref}/{to_ref}")
async def get_quick_stats(repo_path, from_ref, to_ref) -> dict:
    """Быстрая сводка без деталей"""
    # Возвращает: {total_commits, contributors, date_range}
```

### Prompts (2) — Рабочие Процессы

```python
@mcp.prompt()
async def draft_release() -> str:
    """
    Чеклист подготовки релиза:
    1. Спроси номер версии
    2. Проверь теги через git://tags
    3. Вызови analyze_commits
    4. Вызови generate_release_notes
    5. Проверь breaking changes
    """

@mcp.prompt()
async def summarize_changes() -> str:
    """
    Резюме для не-технических стейкхолдеров:
    1. Спроси диапазон версий
    2. Вызови analyze_commits
    3. Сгруппируй по бизнес-категориям
    """
```

---

## Влияние на План Разработки

### Приоритеты для хакатона

```
🔴 MUST HAVE (60% баллов):
├── analyze_commits (полная реализация)
├── generate_changelog (markdown формат)
└── Docker образ + /health endpoint

🟡 SHOULD HAVE (25% баллов):
├── generate_release_notes
└── Resources: git://tags, git://commits

🟢 NICE TO HAVE (15% баллов):
├── Prompts: draft-release
└── Дополнительные форматы (JSON, plain)
```

### Обновлённые итерации

| # | Итерация | Время | Результат |
|---|----------|-------|-----------|
| 1 | Tools: analyze_commits | 3-4ч | ✅ Рабочий инструмент |
| 2 | Tools: generate_changelog | 3-4ч | ✅ Генерация Markdown |
| 3 | Tools: generate_release_notes | 2-3ч | ✅ Release notes |
| 4 | Resources (3) | 2-3ч | ✅ Контекст для LLM |
| 5 | Prompts (2) + полировка | 1-2ч | ✅ Workflow шаблоны |
| 6 | Демо + документация | 2ч | ✅ Готовность к хакатону |

---

## Рекомендации для агентов

### Разработчик Tools
✅ Делай: чёткие названия, структурированный возврат, валидацию  
❌ Не делай: дублирования в Resources, сложной логики в Prompts

### Разработчик Resources
✅ Делай: read-only, простые URI, минимальную обработку  
❌ Не делай: бизнес-логики, модификации данных

### Разработчик Prompts
✅ Делай: чеклисты с шагами, ссылки на Tools, краткие описания  
❌ Не делай: дублирования логики, длинных инструкций

---

## Рефлексия

### Что сработало:
✅ Запросил исследование до начала реализации  
✅ Архитектор нашёл best practices в сообществе MCP  
✅ Чёткие критерии для каждого компонента

### Что улучшить:
⚠️ Нужно было изучить MCP спецификацию раньше  
⚠️ SPEC.md требует обновления с новой архитектурой

---

## Следующий шаг

**Обновить SPEC.md** с новой архитектурой и продолжить Итерацию 2:
- Создать `src/mcp_server/git/parser.py`
- Реализовать `analyze_commits` tool
